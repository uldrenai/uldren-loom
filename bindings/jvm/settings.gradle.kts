plugins {
    // Lets Gradle auto-download a matching JDK toolchain (JDK 22) if one isn't installed.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "uldren-loom"
